# PainterTextureSetsCombiner Configuration
# ──────────────────────────────────────────
# Source directory: where Painter exports per-shader-group textures
SOURCE_DIR = r"D:\Animation_Project\Parser\character\Friston-3\Model\Houdini\texture\test1"

# Destination directory: where merged textures will be saved
DESTINATION_DIR = r"D:\Animation_Project\Parser\character\Friston-3\Model\Houdini\texture\Friston-3_Textures_ver.1"

# Base name used in output filenames
BASE_NAME = "Friston-3"

# Texture patterns — each entry merges all matching source files into one output.
# Format: (glob_pattern, output_filename, default_pixel_color)
#   glob_pattern   — wildcard match against filenames in SOURCE_DIR
#   output_filename — merged result filename (use BASE_NAME + "..." for variable)
#   default_pixel   — hex color for pixels with no data written, #RRGGBBAA or #RRGGBB
#
# Add/remove entries as needed. Order matters: first match wins per pixel.

TEXTURE_PATTERNS = [
    ("*_Diffuse*.png",       BASE_NAME + "_Diffuse_srgbtex_acescg.png",      "#000000FF"),
    ("*_Normal*.png",        BASE_NAME + "_Normal_raw.png",                  "#7F7FFFFF"),
    ("*_Displacement*.png",  BASE_NAME + "_Displacement_raw.png",            "#7F7F7FFF"),
    ("*_Roughness*.png",     BASE_NAME + "_Roughness_raw.png",               "#7F7F7FFF"),
]
